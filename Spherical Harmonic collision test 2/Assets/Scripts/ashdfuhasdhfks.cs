using System;
using System.Collections.Generic;
using System.Linq;
using NUnit.Framework;
using Unity.VisualScripting;
using UnityEngine;

public class ashdfuhasdhfks : MonoBehaviour
{
    public double yourmum = 0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        List<int> l = new List<int>();
        l.Add(0);
        l.Add(69);
        l.Add(420);

        foreach (int i in Enumerable.Range(0, l.Count()))
        {
            print(l[i]);
            yourmum += l[i];
            print("your dads got a hairy minge");
        }
        print(yourmum);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
